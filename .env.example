# === ENV for Render ===
PORT=10000
OPENAI_API_KEY=replace_with_your_openai_key
ALLOWED_ORIGINS=https://your-frontend.vercel.app,http://localhost:3000
MODEL=gpt-4o-mini
MAX_CONCURRENCY=2
PER_REQUEST_DELAY_MS=1200
# Optional: e.g. "true" to enable verbose logs
DEBUG=false

# === Apify Configuration ===
APIFY_API_TOKEN=replace_with_your_apify_token
# Facebook cookies as JSON array string (for Apify actor authentication)
# Format: JSON array of cookie objects with fields like domain, name, value, secure, httpOnly, etc.
# Example: [{"domain":".facebook.com","name":"c_user","value":"123456","secure":true,"httpOnly":false},...]
FACEBOOK_COOKIES=replace_with_facebook_cookies_json_array
