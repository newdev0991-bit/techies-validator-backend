# Literal quote context correction

The three-post canary found a real opening announcement whose model-selected quote,
"we've got a new home", omitted the event terms required by the identity gate.
The independently matched proof page already supplied verified phone/address data.

For an existing literal self-claim only, the shared selector can include its own
paragraph and the immediately following paragraph (500 characters maximum). The
context must name the same independently verified business and contain a self-event.
Missing or invented quotes, unknown/third-party claims, unmatched or inaccessible
pages, disconnected paragraphs, referrals, and other tagged businesses do not
gain this fallback. Both the backend and contact Actor use the same selector.

The chosen proof remains an original contiguous span. Identity output records the
original and expanded quotes; original model quality claims remain unchanged.
Existing contact provenance, conflicts, quality, and 24-hour freshness gates still
control Ready status. No search budget, schedule, or access control is changed.

Saved canary evidence reproduces a matched identity with the original model claim.
This is a deterministic replay, not a new paid validation or demonstrated keyword
yield increase. Publish the matching backend, evidence Actor, and controller, then
run the controller with enabled=false to refresh derived RESULTS from retained
evidence without changing original validation times. Keep the schedule disabled.
