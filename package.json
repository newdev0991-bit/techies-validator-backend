{
  "name": "techies-validator-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/*.test.js",
    "test:pipeline": "node --test pipeline/test/*.test.mjs",
    "test:cloud": "node --test cloud/test/*.test.mjs",
    "pipeline:tick": "node pipeline/cli.mjs tick",
    "pipeline:plan": "node pipeline/cli.mjs plan"
  },
  "dependencies": {
    "apify-client": "^2.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
